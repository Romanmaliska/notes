export type NoteInput = {
  title: string;
  text: string;
};

export type User = {
  name: string;
  email: string;
};

export type SignUpCredentials = {
  name: string;
  email: string;
  password: string;
};

export type LoginCredentials = {
  name: string;
  password: string;
};
